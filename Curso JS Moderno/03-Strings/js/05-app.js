/* const producto = "Monitor 20\"";

console.log(producto.charAt(0)); */

/**
 * .replace(valor a reemplazar, reemplazo) Permite reemplazar una parte del texto
 * .slice(inicio, fin) permite cortar una parte del texto
 * .subString(inicio, fin) igual que slice, pero arregla los indices
 * .charAt(posición) toma un caracter del texto en la posición indicada
*/