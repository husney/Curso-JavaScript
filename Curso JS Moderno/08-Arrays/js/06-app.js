
/* const carrito = [];

const monitor = {
    nombre: "Monitor 25 Pulgadas",
    precio: 300
}

const celular = {
    nombre: "Celular",
    precio: 800
}

const procesador = {
    nombre: "Raizen 5 Xtreme",
    precio: 500
}

const teclado = {
    nombre: "Teclado",
    precio: 50
}



carrito.push(monitor);
carrito.push(procesador);
carrito.push(celular);
carrito.push(teclado);

console.table(carrito);

//Elimina uno o más valores especificando 
//la posición y la cantidad
carrito.splice(1,1);






/* console.table(carrito);

/* //Elimina el elemento final del array
carrito.pop();

//Elimina el elemento al inicio del array
carrito.shift();
 */

//elimina los elementos indicando la posicion y la cantidad
//de elementos que se desean eliminar
//carrito.splice(1, 4);

console.table(carrito);


carrito.push(teclado);
carrito.push(monitor);
carrito.push(procesador); */